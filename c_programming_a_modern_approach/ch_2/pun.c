#include <stdio.h>

// Int means returns an integer
// Void means has no arguments to be received
int main(void)
{
    printf("To C or not to C: that is the question.\n");
    return 0; // Returns a value of 0 
              // and it causes the function to terminate
}